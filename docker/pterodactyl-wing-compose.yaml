version: '3.8'

services:
  setup:
    image: ghcr.io/pterodactyl/wings:v1.11.13
    environment:
      - WINGS_UID=103
      - WINGS_GID=996
      - PANEL_URL=${PANEL_URL}
      - TOKEN=${TOKEN}
      - NODE=${NODE}
    volumes:
      - "config:/etc/pterodactyl/"
    working_dir: /etc/pterodactyl
    command: ["wings", "configure", "--panel-url", "$PANEL_URL", "--token", "$TOKEN", "--node", "$NODE"]
    restart: no

  wings:
    image: ghcr.io/pterodactyl/wings:v1.11.13
    depends_on:
      - setup
    environment:
      - WINGS_UID=103
      - WINGS_GID=996
    volumes:
      - "/etc/timezone:/etc/timezone:ro"
      - "/etc/localtime:/etc/localtime:ro"
      - "/var/run/docker.sock:/var/run/docker.sock"
      - "config:/etc/pterodactyl/"
      - "${LOCAL_VOLUME_STORAGE:-/srv/pterodactyl/volumes}:${LOCAL_VOLUME_STORAGE:-/srv/pterodactyl/volumes}"
    restart: unless-stopped
    networks:
      - traefik
    labels:
      - "traefik.enable=true"
      - "traefik.http.services.pterodactyl-wing.loadBalancer.server.port=80"
      - "traefik.http.routers.pterodactyl-wing-local.entrypoints=http,https"
      - "traefik.http.routers.pterodactyl-wing-local.rule=Host(`pterodactyl-wing.fridge.local`)"

volumes:
  config:

networks:
  traefik:
    external: true
